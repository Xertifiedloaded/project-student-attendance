// SQLite seed removed — project now uses MongoDB (Mongoose).
// To seed MongoDB, use scripts/seed-mongo.js (will be created separately).
console.log('SQLite seed script removed. Use scripts/seed-mongo.js to seed MongoDB Atlas.')
